{
  "name": "mysticat-ci",
  "version": "0.0.0-semantically-released",
  "description": "Reusable GitHub Actions workflows and composite actions for SpaceCat / Mysticat services.",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/adobe/mysticat-ci.git"
  },
  "scripts": {
    "semantic-release": "semantic-release",
    "semantic-release-dry": "semantic-release --dry-run --no-ci --branches $CI_BRANCH"
  },
  "devDependencies": {
    "@semantic-release/changelog": "6.0.3",
    "@semantic-release/git": "10.0.1",
    "semantic-release": "25.0.3"
  }
}
